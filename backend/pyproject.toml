[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for data analysis with Pandas and NumPy"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "pandas>=2.2.0",
    "numpy>=2.0.0"
]
